Сгенерируй solution.py, который принимает в качестве агрумента при запуске абсолютный путь до входного файла в формате как в `tests/translation/*.in` и выводит в stdout результат как в `tests/translation/*.out`
